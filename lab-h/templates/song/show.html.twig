{% extends 'base.html.twig' %}

{% block title %}Song{% endblock %}

{% block body %}
    <h1 class="display-5">Song information</h1>

    <table class="table text-white">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ song.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ song.title }}</td>
            </tr>
            <tr>
                <th>Artist</th>
                <td>{{ song.artist }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ song.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_song_index') }} " class="btn btn-secondary btn-sm mb-3">back to list</a>

    <a href="{{ path('app_song_edit', {'id': song.id}) }}" class="btn btn-secondary btn-sm mb-3">edit</a>

    {{ include('song/_delete_form.html.twig') }}
{% endblock %}
